# Copy to .env.local (gitignored) and set for this machine.
# Root holding the pinned artifacts (ensembles + checkpoints). Must sit OUTSIDE both repos.
QSAE_ARTIFACTS=/path/to/qse-artifacts
QSAE_BACKUPS=/path/to/qse-artifact-backups
